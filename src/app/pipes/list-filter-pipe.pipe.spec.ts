import { ListFilterPipePipe } from './list-filter-pipe.pipe';

describe('ListFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ListFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
